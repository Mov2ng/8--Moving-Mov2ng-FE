import ProfileRegisterContainer from "../(components)/ProfileRegisterContainer";

/**
 * 프로필 등록 페이지
 * @returns
 */
export default function ProfileRegisterPage() {
  return <ProfileRegisterContainer />;
}
