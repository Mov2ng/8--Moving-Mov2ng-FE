import UserProfileEditContainer from "../../(components)/UserProfileEditContainer";

/**
 * UserProfileEditPage: 일반유저 프로필 수정 페이지
 * @returns
 */
export default function UserProfileEditPage() {
  return <UserProfileEditContainer />;
}
